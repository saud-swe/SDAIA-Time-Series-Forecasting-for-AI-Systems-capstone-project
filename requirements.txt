# Local run only. The notebook installs its own packages in its first cell so
# that it stays self-contained on a fresh Colab runtime - this file is for
# cloning the repo and running it locally.
#
#   pip install -r requirements.txt
#   jupyter lab capstone_forecasting_report.ipynb

pandas>=2.0
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
lightgbm>=4.0
statsmodels>=0.14
scipy>=1.10
jupyterlab>=4.0

# Only needed to regenerate/re-execute the notebook via build/
nbformat>=5.9
nbclient>=0.9
